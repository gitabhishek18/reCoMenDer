# Database Notes

Use this folder for database-facing assets if you add them later, for example:

- schema export SQL
- table creation scripts
- seed data scripts
- migration files

No SQL files were introduced during this repository cleanup because the current project already relies on the existing Spring Boot/JPA setup and the request was to avoid adding new features or changing backend logic.
