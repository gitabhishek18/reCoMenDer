# Generated Artifacts

This folder is used for local trainer outputs such as:

- sparse matrices
- similarity files
- popularity outputs
- evaluation files
- metrics snapshots

The large generated files inside this folder should not be committed to Git. Keep them local and regenerate them when needed.
